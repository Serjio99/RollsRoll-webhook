# Server
PORT=3010
NODE_ENV=development

# Webhook protection: this value must be sent in X-Webhook-Secret
WEBHOOK_SECRET=test-secret-123

# Demo mode for customer presentation.
# For local tests we send the prepared Suvvy payload back into this service.
MOCK_SUVVY_ENABLED=true
SUVVY_API_URL=http://localhost:3010/mock-suvvy
SUVVY_API_TOKEN=test-suvvy-token
SUVVY_REQUEST_TIMEOUT_MS=10000

# Production Suvvy bot API goes here instead of the demo URL above:
# SUVVY_API_URL=https://your-suvvy-bot.example/api/frontpad/order-status
# SUVVY_API_TOKEN=real-suvvy-token

# MAX bot API is needed only if this service sends directly to MAX instead of Suvvy.
# In the current architecture MAX stays behind Suvvy, so these are placeholders for the neighboring MAX bot project.
MAX_API_URL=
MAX_BOT_TOKEN=

# Optional: override status messages by status_id as JSON object
# STATUS_MESSAGES_JSON={"3":"Ваш заказ #{order_number} принят.","4":"Ваш заказ #{order_number} готовится."}

